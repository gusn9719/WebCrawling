# 로그
*.log
crawler.log

# OS / filesystem
.DS_Store
Thumbs.db
desktop.ini

# 환경 설정 (민감 정보 포함 가능)
.env
.env.*
secrets.py

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Test / coverage cache
.pytest_cache/
.coverage
htmlcov/

# IDE
.vscode/
.idea/

# 크롤링/전처리 산출물 (대용량 생성 데이터)
output/
output_external/
output_recovered_*/
preprocessed/
preprocessed_v2/

# 전처리 v3 — 대용량 파일만 개별 제외
# (product_recommendation_scores.parquet, product_skin_aggregates.parquet,
#  lstm_v3_preds.parquet, transformer_v3_preds.parquet 는 소용량이므로 포함)
preprocessed_v3/train.parquet
preprocessed_v3/val.parquet
preprocessed_v3/service_reviews.parquet
preprocessed_v3/ambiguous.parquet
preprocessed_v3/*_preview.csv

# 모델 대용량 파일 — Releases 제공 (소용량 tokenizer/vocab/config는 직접 Git)
*.keras
*.h5
*.joblib
*.pkl
*.npy
*.npz
*.pt
*.pth
*.ckpt

# Transformer safetensors (대용량, Releases 제공)
models/transformer_final_v3/model.safetensors
models/transformer_final_v3/training_args.bin

# Transformer 학습 중간 체크포인트 (불필요)
models/transformer_final_v3/checkpoint-*/

# 이전 버전 모델 폴더 (Releases에도 미포함)
models/transformer_final_v2/
models/transformer_final_v3/runs/
models/transformer_klue-bert-base_balanced/

# 이전 버전 BiLSTM 어휘/레이블 파일 (최종 v3 vocab만 직접 Git 포함)
models/lstm_vocab.txt
models/label_map.json
models/lstm_balanced_e3_earlystop_vocab.txt
models/lstm_balanced_e3_earlystop_label_map.json
models/lstm_none_e3_earlystop_vocab.txt
models/lstm_none_e3_earlystop_label_map.json
models/lstm_final_v2_vocab.txt
models/lstm_final_v2_label_map.json

# AI 어시스턴트 가이드 (로컬 전용 — 깃에 올리지 않음)
CLAUDE.md

# 상세 구현 해설 (로컬 전용)
README2*.md

# 오류/실행 로그 파일
*.err
precompute_preds.log

# 백업 폴더
backups/

# 개발 중 임시 확인 파일 (문서 반영 후 제거 예정)
inspect_*.txt
edge_case_analysis.txt

# Release 생성용 아카이브 (로컬 전용, Git 제외)
/oliveyoung_raw_data_v3.tar.gz
