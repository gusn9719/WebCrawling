# Worklog - 2026-06-28

## Summary

- Goal: Step 5 / 5-1 / 5-2 / 5-4A — streamlit_app_v2.py UX 전수 동기화
- Status: Step 5-4A 완료
- Result: 46/46 검수 통과, py_compile OK, HTTP 200 확인

---

## Entries

### 01:30 - Step 5: 피부타입 맞춤 추천 UI 추가

#### What changed

- `streamlit_app_v2.py`에 피부타입 맞춤 추천 기능 추가 (778줄 → 1160줄)
- 탭 구성: 4탭 → 5탭 (피부타입 맞춤 추천 / 일반 상품 추천 / 상품 비교 / 모델·데이터 리포트 / 리뷰 직접 분석), 탭 이름 이모지 제거
- 신규 추가 함수 6개: `load_personalized_recommendation_data()`, `filter_personalized_scores()`, `get_skin_reviews()`, `_get_review_count()`, `_render_review_card()`, `_render_skin_product_detail()`
- 신규 상수: `SCORES_PARQUET`
- ROOT 체크: `BASE_DIR.resolve() != _EXPECTED_ROOT` 불일치 시 `st.error + st.stop`

#### Why

- Step 4에서 생성한 `product_recommendation_scores.parquet`(6008 rows)를 기반으로 피부 타입별 부정 신호 탐색 UI 제공
- 기존 추천 탭(긍정률 60% 공식)과는 별개로, 선택 피부 타입 리뷰 기반 검토 점수를 활용
- service_reviews에서 product_key + base_skin_type 기준으로 부정 리뷰 원문을 먼저 표시

#### Files touched

- `streamlit_app_v2.py` — 신규 탭, 신규 함수, ROOT 체크, 탭 선언 수정
- `streamlit_app_v2_backup_step5.py` — 수정 전 원본 백업 (내용 변경 없음)

#### Decisions

- Decision: 상품 선택을 product_name이 아닌 product_key 기준으로 처리
  - Reason: 동일 product_name이 피부타입별(5종)로 중복 존재 (검수 결과 1,434개 중복)
  - Alternatives considered: product_name + platform 조합으로 uniqueness 확보
  - Why rejected: score_df는 동일 product_key가 피부타입별로 5행 존재하므로 product_key + base_skin_type 조합이 유일 식별자. display_label로 표시하고 내부는 product_key 매핑이 가장 안전

- Decision: service_reviews 로딩 시 columns=None 전체 읽기 금지
  - Reason: 402,438행 × 59컬럼 전체 로딩은 불필요한 메모리 낭비
  - Alternatives considered: columns=None 로딩 후 필요 컬럼 select
  - Why rejected: 두 번 읽는 것과 같은 비용

- Decision: 새 탭 이모지 전면 금지, 기존 탭 내부 이모지 유지
  - Reason: 사용자 요구사항. 기존 탭 내부는 이번 Step 5 수정 범위 밖
  - 탭 이름에서 이모지 제거 확인 (정규식 검증 통과)

#### Commands / Tests

```powershell
# 문법 검증
C:\Users\user\anaconda3\envs\oliveyoung\python.exe -m py_compile streamlit_app_v2.py
# → py_compile OK

# 앱 실행
C:\Users\user\anaconda3\envs\oliveyoung\python.exe -m streamlit run streamlit_app_v2.py --server.port 8501
# → Uvicorn server started on 0.0.0.0:8501

# 수동 검수 스크립트
C:\Users\user\anaconda3\envs\oliveyoung\python.exe step5_verify.py
```

Result:

```
score_df: 6008 rows / service_df: 402438 rows
지성 rank_exposure 후보: 817개
중성 rank_exposure 후보: 0개 (예상대로 — insufficient_evidence 460/490)
display_label 중복: 0건 (product_key 기준 선택 안전)
insufficient_evidence 20개 rank_exposure_flag: 모두 False (OK)
py_compile: OK
streamlit: 기동 확인
새 탭 이름 이모지: 없음 (OK)
product_key 기준 선택 구현: OK (line 709-711)
```

#### Verification

- [x] Code reviewed
- [ ] Tests run (자동 테스트 없음)
- [x] Build passed (py_compile OK)
- [x] Manual check completed
- [ ] Not verified

#### Remaining work

- Chrome extension 불안정으로 브라우저 스크린샷 검증 미완료. 사용자가 직접 http://localhost:8501 접속 후 확인 필요.
- 중성 피부타입은 rank_exposure 후보 0건 → UI에서 include_insufficient=True + only_rank_exposure=False로 필터 조정해야 결과가 표시됨. 사용자 안내 필요.

#### Risks / Notes

- `_render_skin_product_detail` 내부에서 row 파라미터 사용 (selected_row 아님) — 계획대로 구현됨
- 기존 `load_and_aggregate()`, `get_reviews()` 함수 변경 없음 — train/val 402,438행 정상 확인

---

### Step 5-1: 피부타입 맞춤 추천 UX 개선

#### What changed

- Nielsen 10 heuristics 기반 15개 UX 항목 개선
- `_reset_skin_filters()` 콜백 함수 추가 (on_click 방식, 명시적 기본값 대입)
- `_render_skin_product_detail()`: evidence_level·recommendation_tier 값 한국어화, display_message를 st.info로
- `with tab_skin:` 블록: 3단계 흐름 안내, 기본/고급 필터 분리, 브랜드 범위 축소, 활성 조건 요약, sm5 메트릭 한국어화, 빈 상태 복구 힌트, format_func selectbox (product_key 비노출)

#### Why

- 개발자 용어(insufficient_evidence, strong_candidate 등)가 사용자 화면에 그대로 노출됨
- 브랜드 멀티셀렉트에 수백 개 항목, 필터 7개 동시 노출으로 인지 과부하
- 중성 피부 기본값(rank_exposure=True)으로 결과 0건 발생 시 복구 안내 없음
- selectbox에 product_key 문자열이 그대로 노출됨

#### Files touched

- `streamlit_app_v2.py` — UX 개선 (1173줄 → 1272줄)
- `streamlit_app_v2_backup_step5_1.py` — 수정 전 백업

#### Decisions

- Decision: format_func으로 selectbox label과 option 값 분리
  - Reason: product_key는 내부 식별자로 사용자에게 노출 불필요
  - Alternatives: display_label 컬럼 + label_map_skin dict
  - Why rejected: label에 product_key 문자열이 포함되어 UX 저하
- Decision: 필터 초기화에 on_click 콜백 + 명시적 기본값 대입
  - Reason: del 방식은 위젯 렌더링 중 KeyError/상태 불일치 위험
  - Why: Streamlit 공식 권장 방식

#### Verification

- [x] py_compile OK
- [x] HTTP 200 (streamlit 기동 확인)
- [x] 검수 스크립트 66/67 통과 (1건은 DOTALL 오탐)
- [x] product_key 미노출 5개 샘플 확인
- [ ] 브라우저 스크린샷 (Chrome extension 불안정, 직접 확인 필요)

#### Remaining work

- 사용자가 http://localhost:8501 접속 후 피부타입 맞춤 추천 탭 직접 확인

#### Risks / Notes

- 검수 스크립트 FAIL 1건: `re.search(r"1단계.*→", src, re.DOTALL)`가 tab1 주석의 `→`까지 매칭 — 실제 tab_skin 블록 내 `→` 없음(0건) 확인됨

---

### Step 5-2: 피부타입 맞춤 추천 탭 사용 흐름 개선

#### What changed

- 기본 모델 selectbox: index=0 (베이스라인) → `index=_default_model_idx` ("LSTM (BiLSTM)" 기본 선택)
- `get_product_reviews()` 신규 함수 추가 (skin_type 필터 없는 전체 상품 리뷰 조회, label/max_rating 옵션)
- `_render_skin_product_detail()` 부정 리뷰 섹션: 단일 `get_skin_reviews` 호출 → 3단계 fallback + count caption
  - 1단계: 선택 피부타입 부정 리뷰 (get_skin_reviews)
  - 2단계: 전체 상품 부정 리뷰 참고 (get_product_reviews label="negative")
  - 3단계: 낮은 별점 리뷰 참고 (get_product_reviews max_rating=3)
  - caption: "리뷰 조회 기준: 선택 피부타입 N건 / 전체 상품 N건 / 낮은 별점 N건"
- `with tab_skin:` else 블록: st.dataframe에 on_select="rerun" + selection_mode="single-row" 추가
  - 테이블 행 선택 시 session_state["skin_sel_prod"] 갱신 (selectbox 렌더링 전)
  - 필터 변경으로 기존 product_key 목록 이탈 시 session_state도 첫 번째 product_key로 명시적 초기화
  - format_func fallback: str(_k) → "상품 정보 없음" (product_key 노출 차단)

#### Why

- 첫 실행 시 베이스라인 모델이 선택되어 BiLSTM 결과가 보이지 않는 문제
- 테이블 행 클릭이 상세 영역과 연동되지 않아 UX 단절
- 선택 피부타입 부정 리뷰 0건 시 "없습니다" 단정 표시로 오해 유발
- session_state가 필터 변경 후 stale product_key를 유지해 테이블·selectbox·상세 불일치 가능

#### Files touched

- `streamlit_app_v2.py` — 4개 영역 수정 (1272줄 → 1313줄)
- `streamlit_app_v2_backup_step5_2.py` — 수정 전 백업

#### Decisions

- Decision: format_func fallback을 "상품 정보 없음" 고정 문자열로
  - Reason: str(_k) fallback은 product_key를 사용자에게 노출
  - Why: product_key는 내부 식별자, 절대 UI 노출 금지 원칙
- Decision: session_state["skin_sel_prod"]를 필터 변경 시 명시적으로 갱신
  - Reason: index만 0으로 바꾸면 session_state 내 기존 값이 남아 다음 rerun에서 잘못된 상품 선택
  - Why: 테이블·selectbox·상세 영역이 항상 동일 product_key를 참조해야 함

#### Commands / Tests

```powershell
C:\Users\user\anaconda3\envs\oliveyoung\python.exe -m py_compile streamlit_app_v2.py
# → py_compile OK

C:\Users\user\anaconda3\envs\oliveyoung\python.exe step5_2_verify.py
# → 44건 OK / 0건 FAIL

Invoke-WebRequest http://127.0.0.1:8501
# → HTTP 200
```

#### Verification

- [x] Code reviewed
- [ ] Tests run (자동 테스트 없음)
- [x] Build passed (py_compile OK)
- [x] 검수 스크립트 44/44 통과
- [x] HTTP 200 확인
- [ ] 브라우저 UI 클릭 검증 (직접 확인 필요)

#### Remaining work

- 사용자가 http://127.0.0.1:8501 접속 후 피부타입 맞춤 추천 탭에서:
  - 테이블 행 클릭 시 하단 상세 영역 동기화 확인
  - 피부타입 부정 리뷰 0건 상품 선택 시 fallback 섹션 표시 확인

#### Risks / Notes

- 검수 출력 중 CP949 인코딩 오류 발생 (result 문자열의 '—' 문자) — 검수 결과(44 OK / 0 FAIL)에는 영향 없음
- Streamlit 1.58.0 확인 — on_select="rerun" 정상 지원

---

### Step 5-4A: 전역 플랫폼 필터 및 cascade 동기화

#### What changed

- 콜백 3개 추가: `_on_sidebar_platform_change`, `_on_sidebar_category_change`, `_on_skin_cat_change`
- `_reset_skin_filters()`에서 `skin_plat` 초기화 라인 제거
- 사이드바 `sel_platform` radio: `key="sidebar_platform"` + `on_change` 추가
- 사이드바 category: platform 종속 계산, key, on_change, 유효성 검증 추가
- 사이드바 brand: platform+category 종속, key, 유효성 검증 추가
- 사이드바 나머지 위젯 4개: key 추가
- `tab_skin`: `skin_platform` selectbox 제거 → coupang 분기 + else 블록 + platform caption
- `tab_skin` category/brand: sidebar `sel_platform` 기준으로 cascade 변경
- `tab_skin` filter 호출: `skin_platform` → `sel_platform`
- `tab2` 상품 multiselect: `product_name` 기반 → `_compare_key`(product_key 우선, 없으면 platform::product_id), platform 필터, format_func label
- `get_reviews_with_rating()` 신규 helper 함수 추가 (별점 포함 리뷰 조회)
- `tab1` 부정 리뷰: 헤더 "**😞 부정 리뷰**" → "**모델 부정 감지 리뷰 참고**", caption "부정 리뷰 없음" → "모델이 부정으로 감지한 리뷰를 찾지 못했습니다.", 별점 표시 + 고별점 충돌 안내
- `tab2` 부정 리뷰: 동일 헤더/caption 변경, "(없음)" 제거

#### Why

- 사이드바 platform이 tab1에만 흐르고 tab_skin/tab2에 반영되지 않아 필터 불일치
- oliveyoung에 beauty 카테고리 미노출 오류
- tab2 상품명 기반 선택은 동일 상품명 중복 시 잘못된 행 선택 위험
- "부정 리뷰 없음" 단정 표현 + "😞" 이모지가 모델 예측 불확실성을 과장

#### Files touched

- `streamlit_app_v2.py` — 16개 영역 수정 (1313줄 → 1486줄)
- `streamlit_app_v2_backup_step5_4a.py` — 수정 전 백업
- `step5_4a_verify.py` — 검수 스크립트 신규 생성

#### Decisions

- Decision: tab_skin 전체를 `else:` 블록으로 감싸 들여쓰기 변경
  - Reason: coupang 진입 시 필터 UI 전체를 숨겨야 함 (단순 조건 체크로는 불가)
  - Why: 약 200줄 재들여쓰기 — Python 스크립트로 처리

- Decision: `_compare_key` = product_key 우선, 없으면 platform::product_id
  - Reason: tab2는 전체 플랫폼이 섞이는 경우 product_id 단독으로 고유성 보장 불가
  - Why: product_key 컬럼이 stats에 존재하므로 그것을 우선 사용

#### Commands / Tests

```powershell
python -m py_compile streamlit_app_v2.py   # → OK
python step5_4a_verify.py                  # → 46건 OK / 0건 FAIL
Invoke-WebRequest http://127.0.0.1:8501    # → HTTP 200
```

#### Verification

- [x] Code reviewed
- [ ] Tests run (자동 테스트 없음)
- [x] Build passed (py_compile OK)
- [x] 검수 스크립트 46/46 통과
- [x] HTTP 200 확인
- [ ] 브라우저 UI 클릭 검증 (직접 확인 필요)

#### 완료 요약

1. 백업 파일 생성 확인 (streamlit_app_v2_backup_step5_4a.py): OK
2. py_compile 통과: OK
3. HTTP 200 확인: OK
4. sidebar platform = oliveyoung일 때 beauty 미노출 확인: 브라우저 직접 확인 필요
5. sidebar platform = musinsa일 때 beauty만 표시 확인: 브라우저 직접 확인 필요
6. sidebar platform = coupang일 때 tab_skin 피부타입 추천 불가 안내 확인: 브라우저 직접 확인 필요
7. sidebar platform 변경 시 category/brand 초기화 확인: 브라우저 직접 확인 필요
8. tab_skin이 sidebar platform을 반영하는지 확인: 브라우저 직접 확인 필요
9. tab2가 sidebar platform을 반영하는지 확인: 브라우저 직접 확인 필요
10. product_key 화면 비노출 확인: 검수 스크립트 OK
11. tab1/tab2 고별점 모델 negative 충돌 안내 확인: 검수 스크립트 OK
12. 기존 탭 4개 보존 확인: 검수 스크립트 OK
13. "부정 리뷰 없음" 단정 표현 제거 확인 (tab1/tab2): 검수 스크립트 OK
14. tab1/tab2 제목 "모델 부정 감지 리뷰 참고" 표시 확인: 검수 스크립트 OK
15. tab2 내부 선택값 고유성 (_compare_key), product_id/key 화면 비노출 확인: 검수 스크립트 OK

#### Remaining work

- 브라우저에서 sidebar platform 변경 시 category/brand cascade 동작 확인 (직접 http://127.0.0.1:8501 접속)
- tab_skin coupang 분기 UI 확인
- Step 5-4B: tab1 두 key 정리, 사이드바 최대 부정률 상한 100% 변경

#### Risks / Notes

- tab_skin 전체 들여쓰기 +4칸 변경으로 diff가 크나, 로직 변경은 최소
- skin_platform 참조 완전 제거 확인 (검수 스크립트 OK)
