# Worklog - 2026-07-01

## Summary

- Goal: 최종 제출 가능한 상태로 저장소 정리 — 개발 중 버전명·경로·파일명 흔적 제거
- Status: 완료 (commit: 0e78c95)
- Result: 파일명 통일, 로컬 경로 하드코딩 제거, 앱 방어 처리, output/ Git 추적 제거, 문서 통일

---

## Entries

### 작업 전체 - 최종 제출 정리

#### What changed

- `streamlit_app_v2.py` → `streamlit_app.py` (git mv)
- `output/skincare_reviews_backup_0515.jsonl` Git 추적 제거 (git rm --cached)
- `streamlit_app.py` 코드 수정:
  - docstring에서 "v2" 제거
  - `_EXPECTED_ROOT` 로컬 절대경로 강제 체크 삭제 (D:/_WebCrawling/oliveyoung_crawler)
  - `load_and_aggregate`: train/val.parquet 없을 때 빈 DataFrame 반환으로 앱 전체 중단 방지
  - `load_personalized_recommendation_data`: service_reviews.parquet 없을 때 `(score_df, None)` 반환 (이전: `st.stop()`)
  - `_render_skin_product_detail`: `service_df=None` 수신 시 안내 메시지 후 early return
  - tab1: `stats.empty`일 때 데이터 파일 안내 메시지 추가 (`if stats.empty: ... elif filtered.empty: ... else:`)
  - 렌더링 UI: "부정적인 내용을 포함하는 리뷰입니다" → "모델이 부정 신호를 감지했습니다. 실제 의미는 리뷰 본문을 함께 확인하세요."
  - TODO 주석 제거
- 문서 업데이트:
  - README.md: `streamlit_app_v2.py` → `streamlit_app.py` (실행 명령 3곳, 파일 참조 4곳), 디렉터리 구조 `oliveyoung_crawler/` → `WebCrawling/`
  - docs/setup_verification.md: 실행 명령 3곳, 섹션 제목, 디렉터리 구조
  - docs/service_design.md, docs/technical_report.md: 파이프라인 및 파일 참조
  - docs/development_journal.md: 현재 파일을 가리키는 참조 3곳 (`streamlit_app_v2.py` → `streamlit_app.py`)
  - docs/worklog/2026-06-29.md: "Remaining work" → "당시 남은 작업(이후 완료)" 상태 업데이트
- `reports/README.md` 신규 생성: 최종 파일 vs 실험 이력 파일 구분 문서화

#### Why

- 파일명에 `v2`가 남아 있으면 제출물이 개발 중 상태로 보임
- `_EXPECTED_ROOT` 체크는 로컬 경로를 강제 비교해 git clone 후 다른 위치에서 실행하면 앱 전체가 중단됨
- `load_and_aggregate`가 train/val.parquet를 앱 시작 시 강제 로딩 → 모드 A(clone만)에서도 앱 전체 중단
- `output/skincare_reviews_backup_0515.jsonl`은 `.gitignore`에 `output/`으로 제외 처리됐는데 이미 추적 중이었음

#### Files touched

- `streamlit_app.py` (renamed from `streamlit_app_v2.py`) — 코드 6곳 수정
- `README.md` — 실행 명령 및 파일 참조 통일
- `docs/setup_verification.md` — 실행 명령 통일
- `docs/service_design.md` — 파이프라인 파일명 수정
- `docs/technical_report.md` — 파이프라인 및 섹션 헤더 파일명 수정
- `docs/development_journal.md` — 현재 파일 참조 3곳 수정 (이력 참조는 유지)
- `docs/worklog/2026-06-29.md` — Remaining work 완료 상태 업데이트
- `reports/README.md` — 신규 생성

#### Commands / Tests

```bash
git mv streamlit_app_v2.py streamlit_app.py
git rm --cached output/skincare_reviews_backup_0515.jsonl

python -m py_compile streamlit_app.py
# → SYNTAX OK

grep -r "streamlit_app_v2" README.md docs/development_journal.md docs/service_design.md docs/setup_verification.md docs/technical_report.md streamlit_app.py
# → (없음) — backup_step5 이력 참조만 남음 (삭제된 파일의 개발 기록, 정상)

git ls-files | grep streamlit
# → streamlit_app.py (단독)

git ls-files | grep "^output/"
# → (없음 — 정상)
```

#### Verification

- [x] Python 구문 검사 통과 (`py_compile`)
- [x] git ls-files: streamlit 추적 파일 `streamlit_app.py` 단독 확인
- [x] output/ Git 추적 없음 확인
- [x] v2 흔적 grep 결과 없음 (최종 실행 파일·문서 기준)
- [ ] 실제 브라우저 실행 확인 불가 (service_reviews.parquet, 모델 파일 없어 모드 A 이상 불가)

#### Remaining work

- git push (사용자 직접 실행)
- 모드 B/C 실행 확인은 서비스 parquet + 모델 파일 배치 후 사용자가 직접 검증 필요

#### Risks / Notes

- worklog/*.md의 `streamlit_app_v2.py` 참조는 당시 파일명의 이력 기록이므로 수정하지 않음

## Streamlit v2 entry point compatibility

- Recreated `streamlit_app_v2.py` as a thin compatibility wrapper around `streamlit_app.py`.
- Verified both `streamlit_app.py` and `streamlit_app_v2.py` pass `py_compile`.
- Verified `streamlit_app_v2.py` can launch through Streamlit and responds with HTTP 200 on `http://127.0.0.1:8501`.
- `backups/`는 `.gitignore`로 제외됨 — 로컬에만 존재하는 백업 파일들의 v2 참조는 무관함
- `reports/README.md`는 신규 파일이므로 git add 후 커밋했으나, GitHub에 반영은 push 후
- tab2: 데이터 없을 때 빈 멀티셀렉트로 "2개 이상 선택하면 비교가 시작됩니다" 표시 — 별도 안내 메시지 없음 (tab1은 명시적 안내 메시지 있음)
