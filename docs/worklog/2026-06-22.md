# Worklog - 2026-06-22

## Summary

- Goal: Transformer v2 중립 class 성능 개선 — 전처리 수정 + 전체 모델 v3 재학습
- Status: 진행 중 (전처리 실행 중)
- Result: TBD

---

## Entries

### 00:00 - 전처리 라벨링 버그 수정 (v3 데이터 준비)

#### What changed

- `preprocess/config.py`: `POSITIVE_KEYWORDS`에서 `"괜찮"`, `"무난"` 제거
- `preprocess/labeling.py`: `_finalize_label`에 3점(neutral) + mixed text → neutral 유지 처리 추가
- `precompute_preds.py`: DATA_DIR을 `preprocessed_v3`으로, 모델 경로를 v3로, 컬럼명을 v3_pred로 변경
- `streamlit_app_v2.py`: DATA_DIR, 모델 로딩 경로, _PRED_COL 컬럼명 모두 v3으로 변경

#### Why

- Transformer v2가 중립 리뷰("그냥 평범해요")에 긍정 100% 예측하는 문제 분석 결과:
  - "괜찮", "무난"이 POSITIVE_KEYWORDS에 있어 3점 리뷰를 neutral → positive 재분류
  - 3점 + mixed text(긍부정 혼재)가 ambiguous로 제외돼 neutral 데이터 극소 (1.2%)
- 두 버그를 수정해 neutral class를 정상 복원 후 재학습 예정

#### Files touched

- `preprocess/config.py` — POSITIVE_KEYWORDS에서 "괜찮", "무난" 제거
- `preprocess/labeling.py` — `_finalize_label`에 neutral+mixed 처리 추가 (99~107번 줄)
- `precompute_preds.py` — v3 경로/컬럼명으로 업데이트
- `streamlit_app_v2.py` — DATA_DIR, 모델 경로, 예측 컬럼명 v3으로 업데이트

#### Decisions

- Decision: "괜찮", "무난"을 NEGATIVE_KEYWORDS가 아닌 단순 제거
  - Reason: 이 단어들은 긍정도 부정도 아닌 중립 표현. 별도 리스트 추가 없이 제거만 해도 3점 리뷰에서 text_rule_label = "unknown" → neutral 유지로 충분
  - Alternatives considered: NEUTRAL_KEYWORDS 새 리스트 추가
  - Why rejected: 현재 라벨링 로직이 neutral 키워드를 별도로 처리하는 경로가 없어 효과 없음

- Decision: 3점 + mixed → neutral 유지 (기존: ambiguous 제외)
  - Reason: 긍부정이 혼재된 3점 리뷰는 별점 자체가 "보통"임을 뒷받침하는 가장 전형적인 중립 리뷰
  - Alternatives considered: 4-5점 + mixed도 neutral로 처리
  - Why rejected: 4-5점 + mixed는 genuine 모호(별점과 텍스트 충돌)이므로 기존대로 ambiguous 처리가 맞음

- Decision: 기존 preprocessed_v2/ 유지, 새 디렉토리 preprocessed_v3/ 생성
  - Reason: 실험 재현성, 롤백 가능성 보존

#### Commands / Tests

```
C:\Users\user\anaconda3\envs\oliveyoung\python.exe run_preprocess.py --include-external --output preprocessed_v3
```

Result:

```
실행 중 (백그라운드)
```

#### Verification

- [x] Code reviewed (config.py diff, labeling.py diff 직접 확인)
- [ ] Tests run (자동화 테스트 없음)
- [ ] preprocessed_v3/ 생성 확인
- [ ] neutral class 비율 1.2% 초과 확인

#### Commands / Tests (Step 3 완료)

```
python run_preprocess.py --include-external --output preprocessed_v3
```

Result:

```
neutral: 5,657건 (2.1%)  ← v2 대비 +27% (4,471 → 5,657)
train: 219,830건 | val: 54,958건
전체 소요: 582.2초
preprocessed_v3/train.parquet, val.parquet, ambiguous.parquet 생성 확인
```

#### Commands / Tests (Step 4 완료 — LSTM v3)

```
python train_lstm.py --train-path preprocessed_v3/train.parquet --val-path preprocessed_v3/val.parquet --class-weight balanced --run-name lstm_final_v3
```

Result:

```
Epoch 6: val_loss=0.2388 (최적), EarlyStopping(patience=2) → epoch 8에서 중단, 최적 가중치 복원
lstm_final_v3: accuracy=0.9211, macro_f1=0.6631, neutral_recall=0.6643
models/lstm_final_v3.keras 저장 확인
```

#### Remaining work

- Step 5: Transformer v3 재학습 (실행 중, CPU 기준 수 시간 소요)
- Step 6: precompute_preds.py 실행
- Step 7: 앱 검증 (Tab 4 중립 예시 테스트)

#### Risks / Notes

- `preprocessed_v2/`는 변경 없음 — 기존 v2 모델/예측 파일 보존됨
- Transformer v3 재학습 후 precompute_preds.py 실행 필수 (사전 계산 파일 없으면 Tab 1/2에서 fallback 추론)

---

### 데이터 검수 요약 (코딩 전 분석)

실제 데이터를 직접 확인하여 다음 문제를 발견함:

1. **★1~2 + mixed_text 6,475건 억울하게 제외**: 50건 직접 검수 → 48/50건이 명확한 부정 리뷰인데 "효과", "재구매" 등 긍정 키워드가 있어 mixed → ambiguous로 버려짐
2. **"효과가 없" 조사 형태 865건 미포착**: "효과없"은 포착, "효과가 없"은 공백+조사 때문에 미포착
3. **"흡수안됩" 붙여쓰기 28건 미포착**: "흡수 안"은 포착, 붙여쓰기는 미포착
4. **"사지마세요", "최악", "환불", "반품" 등 강한 부정 신호 누락**
5. **"건조한 피부", "건조한 날씨" 등 환경/피부타입 맥락 제외 필요**: "건조" 키워드가 긍정 리뷰에서도 점화됨
6. **"재구매안", "추천안" 붙여쓰기 각 27/23건**: 공백 있는 버전만 있어 붙여쓰기 미포착
7. **"돈낭비" 81건 누락**

OKT stem=True가 "-용","-여","-염" 어미 변형은 자동 처리하므로 별도 키워드 불필요.
"앖어요"(없어요 오타) 37건은 수량 적어 학습 노이즈로 수용.

---

### config.py — NEGATIVE_KEYWORDS 17개 → 55개 확장

**NEGATIVE_KEYWORDS** 주요 추가:
- 효과 부정: "효과가 없", "효과도 없", "효과는 없", "효과 못", "효과를 못"
- 흡수 부정: "흡수되지 않", "흡수안됩"(붙여쓰기), "흡수안돼"(붙여쓰기), "겉돌아", "겉도는"
- 추천 부정: "추천하지 않", "추천 못", "추천안"(붙여쓰기)
- 재구매 부정: "재구매안"(붙여쓰기), "재구매 의사 없"
- 강한 부정: "사지마", "쓰지마", "최악", "환불", "반품", "돈낭비", "아무효과"

**NEGATIVE_CONTEXT_EXCEPTIONS** 2개 → 9개 (맥락 예외):
- "건조한 피부", "건성 피부", "건조한 편", "건조했던", "건조한 날씨" 등

---

### LSTM v3 학습 결과

전처리 v3 데이터로 재학습. EarlyStopping(patience=2): Epoch 4 best.

| Epoch | val_acc | val_loss |
|-------|---------|----------|
| 4 | **0.8930** | **0.2822** ← best |

**Final metrics**: accuracy=0.8930, macro_f1=0.6659, neutral_recall=0.5860  
neutral: precision=0.196, recall=0.586 (class_weight=balanced 효과 — recall↑, precision↓ trade-off 수용)

v2 대비 neutral_recall: 0.291 → **0.586** (2배 개선 — 데이터 품질 개선 효과)

---

### Supplement Negative Low-Rating Expansion

#### Summary

- Updated `supplement_negative.py` so the low-rating supplement crawl defaults to RATING_ASC, rating <= 3.0, and up to 50 new candidate reviews per product.
- Added optional `ReviewCrawler.fetch_all()` limits for maximum collected reviews and maximum rating while preserving its default behavior for other callers.
- Existing `review_id` deduplication remains in both crawler-local `seen_ids` and `ReviewStorage.save()`.

#### Commands / Tests

```
C:\Users\user\anaconda3\envs\oliveyoung\python.exe -m py_compile supplement_negative.py oliveyoung/crawlers/review.py
C:\Users\user\anaconda3\envs\oliveyoung\python.exe supplement_negative.py --help
```

Result: both commands completed successfully.
